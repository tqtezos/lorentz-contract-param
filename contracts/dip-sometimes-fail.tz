# DIP should work fine when code block within does not always fail.

parameter unit;
storage unit;
code { DROP;
       UNIT;
       NIL operation;
       PAIR;
       DIP { PUSH int 5; PUSH bool False; IF { FAILWITH } { DROP } }
       };