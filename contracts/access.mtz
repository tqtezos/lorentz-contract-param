let {
  consumeString :: '[string, ...] -> '[...]
  = {drop};
};

parameter
  ( int
  , string
  , option string
  , or string string
  , list string
  );
storage unit;
code { car;
       dup; access 0 5; isnat; drop;
       dup; access 1 5; consumeString;
       dup; access 2 5; if_none {} { consumeString };
       dup; access 3 5; if_left { consumeString }{ consumeString };
       dup; access 4 5; concat; consumeString;
       drop; unit; nil Operation; pair;
     }
