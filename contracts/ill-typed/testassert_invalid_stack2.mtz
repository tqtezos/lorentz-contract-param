parameter (pair int int);
storage unit;
code {
  car;
  unpair;
  test_assert InvalidStack "Inner code doesn't return Bool. Refer to %[1]"
    {drop; drop;};
  drop;
  drop;
  unit;
  nil operation;
  pair;
};