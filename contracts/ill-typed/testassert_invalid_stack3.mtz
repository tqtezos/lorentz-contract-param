parameter (pair int int);
storage unit;
code {
  car;
  unpair;
  test_assert RemoveEmpty "Remove an element from empty stack"
    {drop; drop; drop;};
  drop;
  drop;
  unit;
  nil operation;
  pair;
};