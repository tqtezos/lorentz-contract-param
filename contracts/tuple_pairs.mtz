parameter ();
storage ();
code {
  drop;
  push (int, int, int) (1,2,3);
  cast (pair int (pair int int));
  drop;
  push ((int %a :r, int %b :s, int %c :t) :q) (1,2,3);
  drop;
  unit; nil operation; pair
}
